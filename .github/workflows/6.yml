name: Scheduled workflow

on:
  schedule:
    - cron: '*/2 * * * *'
  workflow_dispatch:

jobs:
  print-msg:
    runs-on: ubuntu-latest
    steps:
      - name: Say my name
        run: echo "My name is GitHub Actions"
