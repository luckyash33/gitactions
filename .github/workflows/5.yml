name: Scheduled workflow

on: 
  schedule:
    - cron: '30 18 * * mon-fri'

jobs:
  print-msg:
    runs-on: ubuntu-latest
    steps:
      - name: Say my name
        run: echo "My name is GitHub Actions"